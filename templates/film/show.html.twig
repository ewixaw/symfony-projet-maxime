{% extends 'base.html.twig' %}

{% block title %}Film{% endblock %}

{% block body %}
    <div style="position: absolute; top: 0; right: 0; margin: 1em;">
        <a href="{{ path('app_logout') }}" style="text-decoration: none; font-size: 16px;">Logout</a>
    </div>
    
    <h1>Film</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ film.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ film.Nom }}</td>
            </tr>
            <tr>
                <th>Annee_sortie</th>
                <td>{{ film.anneeSortie }}</td>
            </tr>
            <tr>
                <th>Genre</th>
                <td>{{ film.Genre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_film_index') }}">back to list</a>

    <a href="{{ path('app_film_edit', {'id': film.id}) }}">edit</a>

    {{ include('film/_delete_form.html.twig') }}
{% endblock %}
